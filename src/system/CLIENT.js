export const BAR_HEIGHT = 60;
export const NEW_HEIGHT = 48;
export const CLIENT_USER_KEY = 'destiny-user'; // how we store `user` in localStorage
export const CLIENT_EVENTS_KEY = 'destiny-events'; // how we store `events` in localStorage
